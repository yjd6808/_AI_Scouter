# 계획: P7 MCP 구현 (15·16연결·17)

## 목표
- 15: /mcp·/health, 세션, Bearer, 승인 3종, 감사 JSONL, 자르기 64KB, Upstream 스튜디오 1개
- 16: ScouterCore Tool 19개 + Connect 탭(상태·스니펫·복사·재발급) + 리소스 + MakePlugin
- 17: McpInspector P7판 (세션 GridView·호출 VirtualList·직접호출 PropertyGrid)
- 완료 기준: sdk Client 통합 테스트 + 외부 스니펫 연결 + 승인 E2E

## 파일 목록 (약 30개)
- Renderer/Mcp/{Auth,SessionManager,McpCore,ToolInvoker,ApprovalManager,AuditLog,ResultTruncator,UpstreamProxy}.ts
- BuiltIn/ScouterCore/{Tools/*.ts 16개,Resources.ts,Prompts.ts,ConnectSnippets.ts,Views/ConnectView.ts,Layout/Connect.xml} + Config/ConnectSnippets.json
- BuiltIn/McpInspector/{Plugin.json,Index.ts,Layout/Main.xml,Views/*,CallLogBuffer.ts}
- TestApi approval/permission 라우트, Main capture-page IPC

## 검증
1. lint/typecheck/build 녹색
2. Integration: sdk Client로 initialize→list→call→DELETE, 401/403/404
3. E2E: 승인 ask→Allow, directly call ScouterCore__SettingsGet

## 주의
- sdk 저수준 Server API 실측 후 작성 (d.ts 확인)
- Tool 결과 image 블록 규약 (Content 배열)
- 한글 주석 Write 도구로만

## 완료 기록 (2026-09-12)
- 검증: lint/typecheck/build 녹색, unit 150개, e2e 4개 통과
- D-20: Source/Scouter.App/package.json에 type:module 추가. tsx가 App 소스를 CJS로 바꿔 싱글톤(Settings/Gui)이 ESM/CJS 2벌로 뜨던 문제 해결
- D-21: 내장 플러그인은 esbuild 번들 금지, PluginManager.LoadBuiltIn에서 직접 import. 번들하면 Settings/McpHttpServer/ToolRegistry가 복제돼 Tool이 빈 상태에서 동작했음. .cache 잔해 삭제
- D-22: Mcp.test.ts는 node:http 직접 호출(in-process) + McpSdkCheck.ts 자식 프로세스(sdk Client). happy-dom fetch는 CORS/SSE를 막아 in-process SDK 불가
- E2E/Mcp.test.ts: 실 Electron → ScouterCore__ConnectSnippet → 스니펫 파싱 → SDK 재접속 → ThemeList 확인
