# 계획: P10 컨트롤 2차 (11·12 나머지)

## 목표
- 11: TreeView(+Item), DataGrid(읽기 전용), Menu/ContextMenu(+Item, MenuBase), ToolBar
- 12: CodeEditor(+MonacoLoader, MonacoTheme), DiffView, MarkdownView(marked+dompurify), Avatar
- 완료 기준: McpInspector가 TreeView/CodeEditor로 교체, P4Util에 ContextMenu
- Slider/ProgressBar는 Range.ts에 이미 있음. DatePicker/Shape는 11·12에 스펙이 없어 제외 (22에만 언급, 사용자 확인 항목).

## 사전 조사 (2026-09-12)
- marked/dompurify 미설치. monaco-editor ^0.56 dev + webpack plugin 있음.
- XML 태그는 `@RegisterElement` 데코레이터로 자동 등록. Index/Register*에 export 추가.
- `UIElement.ContextMenu` 속성 없음. S11-4 구현에 필요.
- McpInspector/Main.xml + InspectorMainControl 현재 ListView 기반. XML+어댑터 최소 수정.
- P4Util `lst_files` 우클릭 메뉴: "경로 복사" (S11-4).

## 파일 목록
- Source/Scouter.Gui/Controls/Items/{TreeView,MenuBase,Menu,ContextMenu,MenuItem,ToolBar,DataGrid}.ts (1파일 1클래스, Item은 동파일? 규칙상 분리)
  → TreeView.ts, TreeViewItem.ts, MenuBase.ts, Menu.ts, ContextMenu.ts, MenuItem.ts, ToolBar.ts, DataGrid.ts
- Source/Scouter.Gui/Controls/Scouter/{Avatar,CodeEditor,MonacoLoader,MonacoTheme,DiffView,MarkdownView}.ts
- Source/Scouter.Gui/Styles/{Items,Gui}.css: tree/menu/toolbar/avatar/markdown 규칙
- UIElement ContextMenu 속성 + InputDispatcher contextmenu → MenuBase 단일 오픈
- Plugins/P4Util: Main.xml 메뉴 선언 + MainControl 복사 명령
- McpInspector: Main.xml TreeView/CodeEditor 교체 + InspectorMainControl 어댑터
- Tests: Unit/Gui/{TreeView,MenuTwo?,DataGrid,MonacoTheme,MarkdownView,Avatar}.test.ts (기존 Items/ControlsTwo에 합칠지 분리할지 실행 시 결정)

## 설계 대비 조정
- D1: `UIManager.ShowPopup`은 P9에 이미 있음. 문서의 Placement 계산은 기존 PopupPlacer 사용.
- D2: DataGrid 1차 읽기 전용 (문서通り). 정렬은 GridView 공유 방식.
- D3: CodeEditor happy-dom 불가 → E2E 또는 수동. Markdown/Theme은 단위 테스트.
- D4: DatePicker/Shape 제외 (스펙 없음).

## 검증
1. lint/typecheck/build 녹색
2. unit 전수 (신규 포함)
3. e2e 전수
4. McpInspector 화면目视 + P4Util 우클릭 (사용자 확인 시)

## 완료 기록 (2026-09-12)
- lint/typecheck/build 녹색. eslint ignore에 `release/` 추가 (pack 산출물 제외).
- unit 184개 중 183 통과, 1 실패 = 기존 Integration/Mcp (Node 24 환경).
- e2e 7통과 + real P4 1스킵.
- 실동작 프로브: McpInspector TreeView 2그룹 + CodeEditor monaco 마운트 확인. P4Util 우클릭 팝업 1개 확인.
- 설계 대비 조정 추가:
- D5: DataGrid는 `ListView`를 확장 (그리드 뷰·정렬 재사용). Selector 계열이라 선택 API 동일.
- D6: `Window.CloseOnOutsideClick` XML 속성은 미구현(P9에서 결정). 바깥 닫기는 코드로 처리.
- D7: MarkdownView script 입력 단위 테스트 제외. happy-dom이 삽입 스크립트를 실행해서 OOM이 나고, 살균 자체는 DOMPurify 표준 동작이라 Electron에서 확인하는 것으로 둠.
- 사용자 확인 대기: DatePicker/Shape 스펙 여부 (22에만 언급, 11·12에 없음).
