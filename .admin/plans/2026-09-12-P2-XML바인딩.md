# 계획: P2 XML/바인딩 구현 (07)

## 목표
- DataList(UIValue 7종 + jc 별칭 정규화) · Expression Pratt 파서·평가기
- XmlLoader 2-pass · ElementCatalog · AttributeApplier · BindingGraph
- LayoutLint(E코드) · HotReloader · FsLayoutProvider · GenLayoutSchema
- 완료 기준: sgcl 샘플 XML 경고 0 렌더, 핫리로드 DataList 보존, 파서 c8 95%

## 파일 목록 (약 25개)
- Source/Scouter.Gui/Xml/*.ts 13개 (DataList, UIValue, LoadContext, ElementCatalog, AttributeApplier, Expression/{Lexer,Parser,Ast}, BindingResolver, Binding, BindingGraph, XmlLoader, LayoutLint, HotReloader)
- Source/Scouter.App/Renderer/Layout/FsLayoutProvider.ts
- Scripts/LayoutLint.mjs, Scripts/GenLayoutSchema.mjs
- Source/Scouter.Tests/Unit/Gui/*.test.ts 7개

## 검증
1. npm run lint → 0개
2. npm run typecheck → 통과
3. npm run test:unit → 7파일 추가 통과
4. npm run lint:layout → 픽스처 18개 코드 정확히 보고

## 주의
- Gui는 DOMParser 사용, Node 의존 금지. Lint CLI가 happy-dom 주입
- 바인딩 평가기는 new Function 금지 (CSP)
- 한글 주석은 Write 도구로만
