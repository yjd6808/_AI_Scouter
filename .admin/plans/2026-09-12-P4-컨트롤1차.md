# 계획: P4 컨트롤 1차 구현 (09나머지·11·12일부)

## 목표
- 09 나머지: RepeatButton, CheckBox, RadioButton(+GroupScope), TextBox, PasswordBox, Label, RangeBase, ProgressBar, Slider, NumericUpDown, Image
- 11 1차: ItemsControl(+Generator), Selector(+SelectionModel), ListBox(+Item), ComboBox, TabControl(+Item), ListView+GridView, Popup(+Placer), ToolTipService, Expander, GroupBox
- 12 일부: VirtualList, LogView(+RingBuffer), Badge, PropertyGrid(+Editors), ApprovalDialog XML/창 골격
- 완료 기준: McpInspector 화면 구성 가능 + P4Util Main.xml lst_files 렌더

## 파일 목록 (약 35개)
- Source/Scouter.Gui/Controls/*.ts 11개 + Styles/Controls/*.css
- Source/Scouter.Gui/Controls/Items/*.ts 12개 (11 1차)
- Source/Scouter.Gui/Controls/Scouter/{VirtualList,LogView,RingBuffer,Badge,PropertyGrid,PropertyEditors}.ts
- Source/Scouter.App/Renderer/Mcp/ApprovalDialogWindow.ts + Layout/ApprovalDialog.xml (골격)
- Source/Scouter.Tests/Unit/Gui/*.test.ts 8개

## 검증
1. npm run lint/typecheck/build → 녹색
2. npm run test:unit → 추가분 통과
3. P4Util Main.xml 렌더 (GridView Rev/Action/Path) — Harness 수준 확인

## 주의
- ItemTemplate은 코드 함수 (XML DataTemplate 제외)
- TextBox 바인딩 단방향 (TextCommitted에서 DataList.Set)
- ComboBox/Popup은 UIManager Popup 레이어 — ShowPopup 최소 구현 필요
- 200항목 이상 자동 가상화는 VirtualList 연동 지점만 (11 완성 시)
