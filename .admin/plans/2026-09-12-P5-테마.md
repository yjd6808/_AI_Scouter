# 계획: P5 테마 엔진 구현 (13, 37개 제외)

## 목표
- Gui/Theme: Theme 타입·Resolver(defs 해석)·Css 생성·Lint(대비)·Color(HSL)
- App/Renderer/Theme: ThemeLoader(내장+UserDir)·ThemeManager(Apply/System/Density/Typography)
- Theme.schema.json + oc-2.json 1개 + ThemePicker.xml/창 + Bootstrap 연동
- 완료 기준: 피커에서 전환·밀도 변경 즉시 반영, lint 녹색

## 파일 목록 (약 15개)
- Source/Scouter.Gui/Theme/{Theme,ThemeResolver,ThemeCss,ThemeLint,Color}.ts
- Source/Scouter.App/Renderer/Theme/{ThemeManager,ThemeLoader}.ts + Themes/oc-2.json
- Source/Scouter.App/{Config/Theme.schema.json,Renderer/Layout/ThemePicker.xml,Renderer/Shell/ThemePickerWindow.ts}
- Scripts/ThemeLint.mjs
- Source/Scouter.Tests/Unit/Gui/Theme*.test.ts 4개

## 검증
1. npm run lint/typecheck/build → 녹색
2. 테스트 4개 통과
3. E2E: 피커 열기 → 선택 → data-theme 변경 (수동 대체: Test API tree)

## 주의
- 37개 이식은 원본 확보 후 (사용자). oc-2 토큰은 Gui.css 값에서 역산
- 토큰 256개 고정 목록은 P5 최소(core 40개) + 확장 구조. 전부 256 하드코딩은 지양 → 문서와 협의 필요시 보고
- Monaco 연동은 P10 (MonacoTheme.FromTokens 스텁만)
