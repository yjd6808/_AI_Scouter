# 계획: P8 P4Util 구현 (19)

## 목표
- 19: 외부 플러그인 `Plugins/P4Util` Tool 7종 + Main 화면 + Recipes 3종 + 프롬프트
- 완료 기준: Fake로 전 Tool 통과 + E2E fake p4 추출

## 파일 목록
- Plugins/P4Util/{Index.ts, Types.ts, P4Runner.ts, ZtagParser.ts, Tools/*Tool.ts 7개, Views/MainControl.ts, Layout/Main.xml, Recipes/*.md 3개, Settings.schema.json, Plugin.json}
- Source/Scouter.PluginApi/IPluginContext.ts: Exec에 Signal/MaxOutputBytes 추가 (D-23)
- Source/Scouter.App/Renderer/Plugin/PluginContext.ts: 전달 구현
- Source/Scouter.App/Renderer/Services/Process.ts: Windows shell 경유 (D-24)
- Source/Scouter.Tests/{Unit/Plugin/P4Util.test.ts, E2E/P4Util.test.ts, Fixtures/P4/p4.cmd}

## 검증
1. lint/typecheck/build 녹색
2. Unit: ZtagParser + ExtractFiles(FakeRunner) + P4Runner(Fake exec)
3. E2E: --plugin-dir Plugins + PATH fake p4.cmd → P4Util__ExtractFiles Count 2

## 주의
- 내장 플러그인이 아니면 상대 import 번들 복제 문제 없음 (외부는 esbuild 격리 정상)
- 한글 주석 Write 도구로만

## 완료 기록 (2026-09-12)
- 검증: lint/layout/typecheck/build 녹색, unit 154개, e2e 5개 통과
- D-23: ctx.Shell.Exec에 Signal/MaxOutputBytes 추가. P4Runner 취소·64MB 상한이 계약으로 들어감
- D-24: Process.Run Windows에서 shell:true. spawn은 PATHEXT(.cmd)를 못 찾아 fake p4 해결
- 편차1: Styles.css 없음. 번들러가 수집만 하고 주입 기구가 없어 theme 바인딩으로 대체
- 편차2: Harness Pages 없음. Harness에 임의 XML 렌더 기구가 없어 E2E + lint:layout로 대체
- 편차3: E2E에서 UI 클릭 대신 MCP 호출로 검증. /test/type 라우트가 없어 depot 입력 불가, settings 주입 + Tool 호출로 대체
- 편차4: GridView 열은 XML이 아닌 코드로 선언. 속성 요소(ListView.View)는 Grid 정의·텍스트형만 지원
- 편차5: 문서 XML의 Type="Number"/"Boolean"은 Int/Bool로, 작은따옴표 문자열은 백틱으로 수정. 이중 중괄호({{@x} + `s`})가 식 안 참조 문법
- 편차6: P4Util.Extracted 이벤트 없음. UI가 Tool을 직접 호출해 화면을 갱신하므로 불필요
- 미확인: 실제 depot 1~40 동작 (19.9). 사용자 환경에서 확인할 것
